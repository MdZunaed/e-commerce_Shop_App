import 'package:flutter/material.dart';
import 'package:flutter_screenutil/flutter_screenutil.dart';

import 'colors.dart';

final kPadding = EdgeInsets.symmetric(horizontal: 15.w, vertical: 20.h);
final gap = SizedBox(height: 15.h);
final gaph = SizedBox(width: 10.w);
final space = SizedBox(height: 10.h);
const kShadow = BoxShadow(blurRadius: 3, color: kGrey);
final divider = Divider(thickness: 1.1.w);
const processing = Center(child: CircularProgressIndicator());
